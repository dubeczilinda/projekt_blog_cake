include ../views/nav.pug

.container
    .row.col-12
        .card(style="width: 100vh").maincard
            img.card-img-top(src= picture alt=`${title}`)
            .card-body
            h5.card-title= title
                p= content
            a.btn.btn-primary(href="#") Mutasd